# Meal Plan Composer UX/UI R6.5.4 — Substituição (R3)

## Não implementado nesta fase

Nenhum redesign de conteúdo do drawer de trocas (`ExchangeGroupPanel`)
foi feito — o seletor de critério já é acessível (`aria-pressed`,
navegável por teclado, confirmado por teste dedicado pré-existente) e
o motor de equivalência é crítico o suficiente pra não ser tocado sem
uma auditoria dedicada de tempo maior que o orçamento desta fase.

## Gate

`MEAL_PLAN_UI_R6_5_4_SUBSTITUTION: FAIL` (não implementado, mesma
decisão consciente da R6.5.3).
