# R8.2 — Workspace

O workspace mantém uma única `consultation_session` por paciente e organiza o atendimento em sete etapas livres. O CTA da ficha encaminha a sessão retornada pelo domínio existente na URL; trocar de etapa preserva esse identificador.
